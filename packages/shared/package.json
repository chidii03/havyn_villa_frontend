{
  "name": "@havyn/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types + Zod schemas for the Havyn Villa web app (and, later, the Expo app). Consumed as TS source — no build step yet.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
